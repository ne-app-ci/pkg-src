# Ne.app Package Sources

## Abstract

The Ne.app Package Sources, we maintain and update the ports for Ne.app systems.

## Deploy

You can deploy an instance of a ports server by specifying the '-s' option.

```sh
pkg -s localhost:3000 -i devel
```
