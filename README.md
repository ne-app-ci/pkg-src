# Ne.org Ports

## Abstract:

The Ne.org Ports system, we maintain and update the ports for Ne.org systems.

## Deploy:

You can deploy an instance of a ports server by specifying the '-s' option.

```sh
pkg -s localhost:3000 -i src-devel
```


